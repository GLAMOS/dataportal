{% import '_incl/docMacros' as macro %}

<div class="sideBar stickyNav">
  {{ macro.richText(entry.sidebarText) }}
  <ul class="anchorNav">
    {% for item in craft.entries.section('glossar').orderBy('title asc').all() %}
      <li><a href="#{{ item.slug }}">{{ item.title }}</a> </li>
    {% endfor %}
  </ul>
</div>
