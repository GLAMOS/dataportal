{% import '_incl/docMacros' as macro %}

<div class="indexOverview">
  {% for item in craft.entries.section('homeSuperlative').all() %}
    {# note: internalLink is a list and has to be fetched first; ref: https://docs.craftcms.com/v3/entries-fields.html#templating #}
    {% for link in item.internalLink.all() %}
      {{ macro.majorLink( item.wert, item.messgrosse, link, link.slug) }}
    {% endfor %}
  {% endfor %}
</div>
